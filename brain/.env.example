# Cervello Gaia — config (copia in .env e compila; .env è gitignored)

# --- Accesso VPS (tunnel SSH verso il Postgres) ---
VPS_SSH_USER=root
VPS_SSH_HOST=46.225.60.101
# Porta del Postgres esposta sull'host VPS (docker port di supabase_db_maestro-test)
VPS_DB_PORT=54362
# Porta locale su cui apriamo il tunnel (lato PC)
LOCAL_DB_PORT=15432

# --- Credenziali DB (lato remoto, raggiunte via tunnel) ---
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=postgres

# --- Telegram ---
GROUP_CHAT_ID=-5260854010

# --- Repo Ecological Leaving (workspace read-only dell'Agent SDK) ---
EL_REPO_PATH=C:\Users\KreshOS\Documents\00-Progetti\ecologicaleaving

# --- Comportamento cervello ---
POLL_INTERVAL_SEC=60
CONTEXT_MSGS=20
MODEL=claude-sonnet-4-6
